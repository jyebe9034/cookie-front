import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ef4444',
    },
    naver: {
      main: '#02d564',
      dark: '#019e4a',
    },
    kakao: {
      main: '#ffdf02',
      dark: '#b59e02',
    },
  },
});

export default theme;
